


class Student:(self, name, age, track, score,)
    # [assignment] Skeleton class. Add your code here
def __init__(self):
        self.name = str(name)
        self.age = int(age)
        self.track = list(track)
        self.score = float(score)

def change_name(self,new_name):
        self.change_name = new_name
        print("student name is", name)
def change_age(self,new_age):
        self.change_name = new_age
        print("the age is",new_age)

def add_track(self,new_track):
        self.change_track = new_track
        print(" the track is", new_tracks)

def get_score(self,new_score):
        self.get_score = new_score
        print("the students score is",new_score)
    
